# Clients

Vacío a propósito. Arrancan cuando `Deck.Api` esté estable:

- `WebDeck` — front-end web que replica el Virtual Deck, consume `Deck.Api` (Fase 7).
- `MobileDeck` — .NET MAUI o Blazor Hybrid, misma `Deck.Api` (Fase 8).
